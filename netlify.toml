[build]
  publish = "."

# Forzamos a que el servidor entregue los JSON
[[headers]]
  for = "/datos/*"
  [headers.values]
    Content-Type = "application/json"
    Access-Control-Allow-Origin = "*"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200